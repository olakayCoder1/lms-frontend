export type BRAND = {
  logo: string;
  name: string;
  visitors: number;
  revenues: string;
  sales: number;
  conversion: number;
};

export type STUDENTBRAND = {
  logo: string;
  code: string;
  name: string;
  downloads: number;
  conversion: number;
};
